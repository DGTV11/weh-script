1) finish the tutorials
  - STOPPED AT: https://www.youtube.com/watch?v=MzuzWupOFDg&list=PLZQftyCk7_SdoVexSmwy_tBgs7P0b97yD&index=12
2) finish up good goods 
  - remember to add append() function in std for interpreter
  - prevent var redeclaration (instead put a separate '=' rule alongside var assign in expr -> 
    - make variable assign/update node
    - make item assign/update node
  - add OOP+file I/0? https://craftinginterpreters.com/the-lox-language.html#classes
    - this means u need to add get and del for member access operator `.`
  - add AST optimiser?
  - common C stdlib (may be inefficient/not exactly best idea but weh...)!
    - then need a better build system (make?)
  - saner go CLI!
2) make bytecode compiler + reg-based VM (give option to choose between AST tree-walk interpreter and bytecode interpreter, especially when running .weh script)
3) make language bundled thingy (which can be compiled to portable executable) or stick with bytecode files

optional (if i rlly want typing??):
- STATIC TYPING!!! 
   - if u do this include type info as in https://pkg.go.dev/reflect#example-TypeOf ? (or simply `reflect.TypeOf(res).String()`) + prevent var type change in for loop)
- add type inference?
