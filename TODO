1) finish up good goods 
  - add type casting
  - add hex int literal in lexer
  - add OOP? https://craftinginterpreters.com/the-lox-language.html#classes
    - this means u need to add get and del for member access operator `.`
  - add directory I/O
  - add import statements into "immutable objects" (?!? if u do this for compatibility u need to make stdlib IN `.weh` files or find some way to make go plugin func return!)
  - add AST optimiser (or just wait until bytecode compiler)?
  - explain syntax/CLI in TODO or smth
2) make bytecode compiler + reg-based VM (give option to choose between AST tree-walk interpreter and bytecode interpreter, especially when running .weh script)
  - maybe (PLT-inspired) trap style stdlib?
3) make language bundled thingy (which can be compiled to portable executable) or stick with .wvn ('wyvern'?) bytecode files
